{
  "//": [
    "SLIDE 23 - THIS FILE IS COMMITTED TO GIT, SO IT CONTAINS NO SECRETS.",
    "",
    "Notice what is missing: the connection string is empty and there is no",
    "Jwt:SigningKey at all. Both arrive from OUTSIDE the repository -",
    "user-secrets in development, platform environment variables in production.",
    "The application refuses to start without them, on purpose.",
    "",
    "Safe to keep here: hostnames, timeouts, page sizes, business rules.",
    "Never here: passwords, API keys, signing keys, connection strings."
  ],

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },

  "AllowedHosts": "*",

  "ConnectionStrings": {
    "LankaMartDb": ""
  },

  "Jwt": {
    "Issuer": "LankaMart.Api",
    "Audience": "LankaMart.Client",
    "AccessTokenMinutes": 15,
    "RefreshTokenDays": 7
  },

  "LankaMart": {
    "MaxPageSize": 100,
    "DefaultPageSize": 20,

    "FreeDeliveryThreshold": 10000,
    "DeliveryFee": 350,
    "CashOnDeliverySurcharge": 250,

    "BcryptWorkFactor": 12,

    "AllowedOrigins": [],

    "ApplyMigrationsOnStartup": false,
    "SeedDemoData": false,
    "EnableTeachingEndpoints": false
  }
}
